const name = 'Anna';
console.log(0 ?? '!');

/*
 * ?. - оператор опциональной последовательности (чтобы избежать ошибок в корнер кейсах)
 * ?? - аналог || только пропускает всё кроме null / undefined
 */
